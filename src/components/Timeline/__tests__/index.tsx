import React from "react";
import { render } from "@testing-library/react";
import Timeline from "..";
import { useAllProjectsNotPaginatedQuery } from "../../../../generated/graphql";

test("ensure that the children of project-selectors and project-events are the same.", () => {
  // const data = useAllProjectsNotPaginatedQuery();
  // const {timelineContainer} = render(<Timeline data={}/>);
});
