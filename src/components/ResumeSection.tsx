import { Box } from "@chakra-ui/react";
import React from "react";

interface ResumeSectionProps {}

export const ResumeSection: React.FC<ResumeSectionProps> = ({}) => {
  return <Box>Resume Section</Box>;
};
